# Testing

`requirements_dev.txt` contains packages for running tests.

## Running tests

For `mypy`:

```python
mypy src
```

For `flake8`:

```python
flake8 src
```

For `pytest`:

```python
pytest .
```
